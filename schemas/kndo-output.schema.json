{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Envelope",
  "description": "The full `--format json` envelope shape — also the schema\ngenerator's root type (`cargo xtask gen-schema`, gated behind the `schema` feature): the\nJSON Schema is derived from this struct, not maintained as a second hand-written document.\nBorrows the run's collections instead of cloning them: `to_json` on a 75k-finding result\nwas paying a full deep clone (every String in every Finding) purely to serialize — the\nborrow makes serialization allocation-free on the input side. Schema output is unaffected\n(schemars sees through references and slices to the same shapes).",
  "type": "object",
  "properties": {
    "baseline": {
      "anyOf": [
        {
          "$ref": "#/$defs/BaselineSummary"
        },
        {
          "type": "null"
        }
      ]
    },
    "diagnostics": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/Diagnostic"
      }
    },
    "findings": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/Finding"
      }
    },
    "fixed": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/Finding"
      }
    },
    "health": {
      "anyOf": [
        {
          "$ref": "#/$defs/Health"
        },
        {
          "type": "null"
        }
      ]
    },
    "kndo_version": {
      "type": "string"
    },
    "run": {
      "$ref": "#/$defs/RunInfo"
    },
    "schema_version": {
      "type": "string"
    },
    "suppressed": {
      "$ref": "#/$defs/SuppressedSummary"
    }
  },
  "required": [
    "schema_version",
    "kndo_version",
    "run",
    "findings",
    "suppressed",
    "diagnostics"
  ],
  "$defs": {
    "AdapterRunInfo": {
      "description": "One registered adapter's contribution (`run.adapters[]`).",
      "type": "object",
      "properties": {
        "files": {
          "type": "integer",
          "format": "uint",
          "minimum": 0
        },
        "id": {
          "type": "string"
        }
      },
      "required": [
        "id",
        "files"
      ]
    },
    "BaselineSummary": {
      "description": "`baseline` summary (the `baseline` envelope field):\n`acknowledged` counts baseline entries that still match a current finding (excluded from\n`findings` and from `--fail-on`); `stale` counts entries that match nothing anymore — the\nunderlying issue was fixed, and `kndo baseline --update` would drop them.",
      "type": "object",
      "properties": {
        "acknowledged": {
          "type": "integer",
          "format": "uint",
          "minimum": 0
        },
        "stale": {
          "type": "integer",
          "format": "uint",
          "minimum": 0
        }
      },
      "required": [
        "acknowledged",
        "stale"
      ]
    },
    "Confidence": {
      "description": "Per-edge strength. Ordered by strength: `Possible < Probable < Certain`, so `max()` yields\nthe strongest evidence and \"edges at least as strong as τ\" is a simple `>=`.",
      "type": "string",
      "enum": [
        "possible",
        "probable",
        "certain"
      ]
    },
    "Delta": {
      "description": "A finding's place in a diff-mode delta. `None`\nin full mode — there is no \"before\" to compare against, so the concept doesn't apply, and\nthe field is omitted rather than forced to some default.",
      "type": "string",
      "enum": [
        "new",
        "fixed"
      ]
    },
    "DeltaOrigin": {
      "description": "Only set on `Delta::New` findings: does this finding sit *inside* the change set itself\n(`Introduced` — dead on arrival, an agent or author can self-correct before committing) or\ndoes it live in untouched code that flipped because of the change at a distance (`Derived`)?",
      "type": "string",
      "enum": [
        "introduced",
        "derived"
      ]
    },
    "Diagnostic": {
      "type": "object",
      "properties": {
        "level": {
          "$ref": "#/$defs/DiagnosticLevel"
        },
        "message": {
          "type": "string"
        },
        "path": {
          "description": "The file this diagnostic is about, when there is one — `None` for project-level\ndiagnostics (e.g. \"cannot walk the project root\"). A diagnostic merged from many\nfiles without this field would be unattributable; adapters emit diagnostics scoped\nto the file they're extracting, the core fills this in.",
          "anyOf": [
            {
              "$ref": "#/$defs/ProjectPath"
            },
            {
              "type": "null"
            }
          ]
        },
        "span": {
          "anyOf": [
            {
              "$ref": "#/$defs/Span"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "required": [
        "level",
        "message"
      ]
    },
    "DiagnosticLevel": {
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "warn",
            "info"
          ]
        },
        {
          "description": "The run could not do what was asked (the exit-2 tier): a requested mode is\nimpossible (`--diff` base that doesn't resolve), not merely degraded. Frontends exit 2\nwhen any error-level diagnostic is present — reporting zero findings because the\nanalysis never ran must never read as a clean pass (a Warn here\nwould let a typo'd base ref fail open in CI).",
          "type": "string",
          "const": "error"
        }
      ]
    },
    "Finding": {
      "description": "Typed form of the output-schema finding (every field lands in the JSON schema\nfirst — that document is normative). Not yet present:\n`evidence` (category-specific block), `sources`, `remediation`, `rolled_up` — each needs\ninfrastructure that doesn't exist yet (computed remediation text) and is omitted\nrather than fabricated with a placeholder.",
      "type": "object",
      "properties": {
        "advisory": {
          "description": "The severity channel: `true` = this finding never influences exit codes or\nbudgets, whatever its `severity` says — the state of every plugin-contributed finding\n(`plugin:` categories) without an explicit `[plugins.gate]` opt-in. Always `false` for\ncore findings; serialized only when true (additive).",
          "type": "boolean"
        },
        "category": {
          "type": "string"
        },
        "confidence": {
          "$ref": "#/$defs/Confidence"
        },
        "delta": {
          "anyOf": [
            {
              "$ref": "#/$defs/Delta"
            },
            {
              "type": "null"
            }
          ]
        },
        "delta_origin": {
          "anyOf": [
            {
              "$ref": "#/$defs/DeltaOrigin"
            },
            {
              "type": "null"
            }
          ]
        },
        "group": {
          "type": "string"
        },
        "id": {
          "type": "string"
        },
        "location": {
          "$ref": "#/$defs/Location"
        },
        "message": {
          "type": "string"
        },
        "related": {
          "description": "Evidence chain — empty for analyses that haven't adopted it yet.\n`default` isn't for deserialization (Finding is serialize-only) — it tells the schema\ngenerator the field is optional, matching the skip-when-empty serialization.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/RelatedLocation"
          }
        },
        "severity": {
          "$ref": "#/$defs/Severity"
        },
        "subject_kind": {
          "type": "string"
        }
      },
      "required": [
        "id",
        "category",
        "group",
        "subject_kind",
        "severity",
        "confidence",
        "message",
        "location"
      ]
    },
    "Health": {
      "description": "The health block. `previous` is filled by the engine (before-side in diff modes, the\nstored snapshot in full mode) — computation here is always \"this graph, now\".",
      "type": "object",
      "properties": {
        "categories": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/HealthCategory"
          }
        },
        "grade": {
          "type": "string"
        },
        "packages": {
          "description": "Per-package breakdown — present when more than one package owns claimed\nfiles; the single-package (and no-manifest) common case omits it.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/PackageHealth"
          }
        },
        "previous": {
          "anyOf": [
            {
              "$ref": "#/$defs/HealthSummary"
            },
            {
              "type": "null"
            }
          ]
        },
        "score": {
          "description": "0–100, one-decimal precision.",
          "type": "number",
          "format": "double"
        }
      },
      "required": [
        "score",
        "grade",
        "categories"
      ]
    },
    "HealthCategory": {
      "type": "object",
      "properties": {
        "category": {
          "type": "string"
        },
        "count": {
          "description": "Subjects counted by the numerator (dead symbols, orphan files, misdeclared deps, …).",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint",
          "minimum": 0
        },
        "coverage": {
          "description": "Ingested coverage provenance (\"coverage-lcov coverage/lcov.info (2d old)\"), or \"none\".",
          "type": [
            "string",
            "null"
          ]
        },
        "crapload": {
          "description": "Σ CRAP score over threshold-exceeding functions (the hotlist's total load).",
          "type": [
            "number",
            "null"
          ],
          "format": "double"
        },
        "penalty": {
          "type": "number",
          "format": "double"
        },
        "ratio": {
          "description": "The raw ratio (numerator / denominator), before the saturation curve.",
          "type": "number",
          "format": "double"
        },
        "tokens_duplicated": {
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        }
      },
      "required": [
        "category",
        "ratio",
        "penalty"
      ]
    },
    "HealthSummary": {
      "type": "object",
      "properties": {
        "grade": {
          "type": "string"
        },
        "score": {
          "type": "number",
          "format": "double"
        }
      },
      "required": [
        "score",
        "grade"
      ]
    },
    "Location": {
      "description": "Where a finding points. Every field is optional because not\nevery subject has all of them: `version-skew`/`duplicate` findings span multiple manifests\nor files, so no single `path` is *the* location — expressing that properly is the `related`\nevidence chain, not yet implemented (deferred, not faked with an arbitrary first path).",
      "type": "object",
      "properties": {
        "package": {
          "type": [
            "string",
            "null"
          ]
        },
        "path": {
          "anyOf": [
            {
              "$ref": "#/$defs/ProjectPath"
            },
            {
              "type": "null"
            }
          ]
        },
        "range": {
          "anyOf": [
            {
              "$ref": "#/$defs/Span"
            },
            {
              "type": "null"
            }
          ]
        },
        "symbol": {
          "description": "The finding's primary named subject — a code symbol's name for symbol-kind findings,\nbut also a dependency's name for `undeclared`/`version-skew` (which have no code symbol\nat all): whatever single name a reader or the agent-format renderer would point at.",
          "type": [
            "string",
            "null"
          ]
        }
      }
    },
    "PackageHealth": {
      "type": "object",
      "properties": {
        "grade": {
          "type": "string"
        },
        "package": {
          "type": "string"
        },
        "score": {
          "type": "number",
          "format": "double"
        }
      },
      "required": [
        "package",
        "score",
        "grade"
      ]
    },
    "ProjectPath": {
      "description": "Project-relative path with `/` separators, the only path form that crosses the adapter\nboundary (case handling and symlink resolution are the core's discovery concern).",
      "type": "string"
    },
    "RelatedLocation": {
      "description": "One entry of a finding's evidence chain (the schema's `related` — what `kndo\nexplain` renders): a concrete location plus its role in the story. Populated by\n`cyclic` (\"a shortest cycle path in `related` as the evidence chain\"); other\nanalyses adopt it as they gain evidence models — never fabricated as a placeholder.",
      "type": "object",
      "properties": {
        "note": {
          "type": [
            "string",
            "null"
          ]
        },
        "path": {
          "$ref": "#/$defs/ProjectPath"
        },
        "range": {
          "anyOf": [
            {
              "$ref": "#/$defs/Span"
            },
            {
              "type": "null"
            }
          ]
        },
        "role": {
          "description": "What this location contributes: \"cycle-hop\", \"cause\", …",
          "type": "string"
        }
      },
      "required": [
        "role",
        "path"
      ]
    },
    "RunInfo": {
      "description": "Owned mirror of the JSON envelope's `run` object — not borrowed, unlike a hot-path type,\nbecause this exists purely to be serialized (and, behind `schema`, to derive the JSON\nSchema from): the one-time clone per `--format json` invocation is free by comparison.",
      "type": "object",
      "properties": {
        "adapters": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/AdapterRunInfo"
          }
        },
        "base_ref": {
          "type": [
            "string",
            "null"
          ]
        },
        "cache": {
          "type": "string"
        },
        "duration_ms": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "mode": {
          "type": "string"
        },
        "project_root": {
          "type": "string"
        },
        "started_at": {
          "type": "string"
        }
      },
      "required": [
        "mode",
        "started_at",
        "duration_ms",
        "cache",
        "project_root",
        "adapters"
      ]
    },
    "Severity": {
      "description": "A finding's severity — each category carries one as\na fixed default; `--strict` promotion isn't implemented yet, so this is always the default.\nDeclaration order doubles as sort/triage order: worst first.",
      "type": "string",
      "enum": [
        "error",
        "warning",
        "info"
      ]
    },
    "Span": {
      "description": "1-indexed line/column span, `start` inclusive, `end` exclusive. Serializes as the\n`[line, col]` pair shape the output schema uses, not an\nobject — tuples serialize as JSON arrays by default.",
      "type": "object",
      "properties": {
        "end": {
          "type": "array",
          "maxItems": 2,
          "minItems": 2,
          "prefixItems": [
            {
              "type": "integer",
              "format": "uint32",
              "minimum": 0
            },
            {
              "type": "integer",
              "format": "uint32",
              "minimum": 0
            }
          ]
        },
        "start": {
          "type": "array",
          "maxItems": 2,
          "minItems": 2,
          "prefixItems": [
            {
              "type": "integer",
              "format": "uint32",
              "minimum": 0
            },
            {
              "type": "integer",
              "format": "uint32",
              "minimum": 0
            }
          ]
        }
      },
      "required": [
        "start",
        "end"
      ]
    },
    "SuppressedSummary": {
      "description": "`suppressed.inline`/`suppressed.config` — always present\n(unlike `baseline`, which is `None` when the feature isn't adopted at all): inline pragma\nmatching runs on every check, so `{ inline: 0, config: 0 }` is a meaningful \"nothing\nsuppressed,\" not an absent subsystem. `config` stays honestly `0` — no `kndo.toml`\nsuppression parser exists yet (the config mechanism).",
      "type": "object",
      "properties": {
        "config": {
          "type": "integer",
          "format": "uint",
          "minimum": 0
        },
        "inline": {
          "type": "integer",
          "format": "uint",
          "minimum": 0
        }
      },
      "required": [
        "inline",
        "config"
      ]
    }
  }
}
