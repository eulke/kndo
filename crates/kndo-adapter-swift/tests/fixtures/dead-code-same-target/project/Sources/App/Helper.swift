class Helper {
    // No import needed — same-target `unit` resolution finds this from main.swift.
    func live() {
    }

    // Never called from anywhere — genuinely dead.
    func dead() {
    }
}
