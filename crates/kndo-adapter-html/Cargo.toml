[package]
name = "kndo-adapter-html"
description = "HTML adapter: an HTML document is an entry point, not a module — it roots itself and names the modules and stylesheets a browser loads with it."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish.workspace = true

[dependencies]
kndo-core = { path = "../kndo-core" }
kndo-adapter-toolkit = { path = "../kndo-adapter-toolkit" }
smol_str = { workspace = true }

[dev-dependencies]
rustc-hash = { workspace = true }
