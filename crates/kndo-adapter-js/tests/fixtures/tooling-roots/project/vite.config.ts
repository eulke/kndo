// Tooling-role by name convention (*.config.*) — a role-derived tooling root (build/config
// scripts are tooling roots), its exported default being the tool's interface.
export default {
  build: { outDir: "dist" },
};
