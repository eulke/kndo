// Tooling-role by name convention (*.config.*) — a role-derived tooling root (RFC 0005 §2:
// "Tooling roots — build/config scripts"), its exported default being the tool's interface.
export default {
  build: { outDir: "dist" },
};
