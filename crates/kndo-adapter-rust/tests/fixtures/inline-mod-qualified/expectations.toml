# inline-mod-qualified: a call qualified by an inline module (`convert::usize("x")`) reaches the fn inside `mod convert { }`.

[[alive]]
subject = "src/defs.rs#usize"
why = "run calls it as convert::usize through the inline module"

[[alive]]
subject = "src/defs.rs#convert"
why = "the inline module that qualified call names"

[[alive]]
subject = "src/defs.rs#run"
why = "main calls defs::run()"
