[project]
name = "app"
version = "1.0.0"
dependencies = [
    "werkzeug>=3.1.0",
    "click>=8.1.3",
    "celery[redis]",
]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
