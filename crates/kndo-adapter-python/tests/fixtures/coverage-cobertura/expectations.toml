# coverage-cobertura: a coverage.py Cobertura report maps onto functions — partial coverage of a complex function is `crap`, zero coverage is `untested`.

[[dead]]
subject = "src/classify.py#classify"
category = "crap"
why = "complexity 10 with only its first branch executed (lines 1-3 of 16 hit in coverage.xml) — a CRAP score of 75"

[[dead]]
subject = "src/dark.py#never_run"
category = "untested"
why = "coverage.xml records zero hits on every line of never_run"

[[alive]]
subject = "src/classify.py#classify"
category = "untested"
why = "test_negative executes its first branch — partially covered, not untested"

[[alive]]
subject = "src/classify.py#label"
category = "untested"
why = "test_label executes both branches"

[[alive]]
subject = "src/classify.py#label"
category = "crap"
why = "complexity 2 and fully covered — far below the CRAP threshold"
