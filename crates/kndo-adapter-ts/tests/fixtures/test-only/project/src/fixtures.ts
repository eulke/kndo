// Imported only by core.test.ts — never by production code. A whole test-only *file*
// (a production-role file whose every incoming edge comes from test roots is
// `test-only`, subject file).
export const sample = { id: 1 };
