# generated-file: both halves of the generated verdict. What the file DECLARES
# is the generator's — `Query`, which nothing imports, is not accused. The FILE
# is this project's: nothing imports it either, and being generated is not a
# reason to keep dead weight in the tree, so the whole-file accusation stands.

[[alive]]
subject = "schema.generated.ts#Query"
category = "unused"
why = "a generator wrote it: the name is the generator's, not this project's to answer for"

[[dead]]
subject = "schema.generated.ts"
category = "unused"
why = "nothing imports it; being a generator's output is not a reason to keep it"
