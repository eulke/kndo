package demo.waste

// Parses fine, imported by nobody, `internal` so no published surface keeps it: dead by
// every rule the engine has. It is here to carry the claim the broken file cannot.
internal fun wasted(): Int = 0
