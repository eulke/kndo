package demo

// The project's own entry, naming neither class below: without one root the
// judgment abstains (a graph nothing reaches would report every file), and
// then the proof could not tell what the pack changed.
fun main() {
    println("up")
}
