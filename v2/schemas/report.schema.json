{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Report",
  "type": "object",
  "properties": {
    "abstained": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/Abstention"
      }
    },
    "base_health": {
      "description": "In the diff modes, the BASE tree's health — a pure function of the tree\nthe invocation pinned, never cross-run state. Absent in `full` mode.",
      "anyOf": [
        {
          "$ref": "#/$defs/Health"
        },
        {
          "type": "null"
        }
      ]
    },
    "baselined": {
      "description": "Current findings the baseline already carries — counted, not repeated.",
      "type": "integer",
      "format": "uint32",
      "minimum": 0
    },
    "diagnostics": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/ReportDiagnostic"
      }
    },
    "findings": {
      "description": "New relative to the baseline; all current findings when none exists.",
      "type": "array",
      "items": {
        "$ref": "#/$defs/Finding"
      }
    },
    "fixed": {
      "description": "Baseline entries the current run no longer produces.",
      "type": "array",
      "items": {
        "$ref": "#/$defs/Finding"
      }
    },
    "health": {
      "description": "The project-level aggregate over the CURRENT tree — the baseline hides\nfindings from the listing below, never from health. Absent when\nreachability itself abstained (the abstention entry says why).",
      "anyOf": [
        {
          "$ref": "#/$defs/Health"
        },
        {
          "type": "null"
        }
      ]
    },
    "plugins": {
      "description": "One entry per ACTIVE plugin, in registration order — what it asserted, what\nmissed, whether its content budget cut. Always present, so a plugin that\ncontributed nothing is visibly distinct from a plugin that never ran.\nJSON key `plugins` is envelope contract (schema `kndo-v2/m6`) — the key\npredates the conduct renaming and stays; the TYPE carries the new name.",
      "type": "array",
      "items": {
        "$ref": "#/$defs/Contribution"
      }
    },
    "run": {
      "$ref": "#/$defs/RunInfo"
    },
    "suppressed": {
      "$ref": "#/$defs/SuppressedSummary"
    }
  },
  "required": [
    "run",
    "findings",
    "fixed",
    "baselined",
    "abstained",
    "suppressed",
    "plugins",
    "diagnostics"
  ],
  "$defs": {
    "Abstention": {
      "type": "object",
      "properties": {
        "category": {
          "$ref": "#/$defs/Category"
        },
        "reason": {
          "$ref": "#/$defs/AbstentionReason"
        },
        "scope": {
          "$ref": "#/$defs/AbstentionScope"
        }
      },
      "required": [
        "category",
        "reason",
        "scope"
      ]
    },
    "AbstentionReason": {
      "oneOf": [
        {
          "type": "object",
          "properties": {
            "streams-not-declared": {
              "type": "object",
              "properties": {
                "missing": {
                  "type": "array",
                  "items": {
                    "$ref": "#/$defs/EvidenceStream"
                  }
                }
              },
              "required": [
                "missing"
              ]
            }
          },
          "additionalProperties": false,
          "required": [
            "streams-not-declared"
          ]
        },
        {
          "description": "No root anchors anything in the whole graph. Reachability judged from zero\nroots would accuse every file at once; that is a missing-evidence condition,\nnot a verdict.",
          "type": "string",
          "const": "no-roots-anywhere"
        },
        {
          "description": "No test root anchors anything: with zero test evidence, \"tests never reach\nthis\" describes every declaration equally and accuses none.",
          "type": "string",
          "const": "no-test-roots-anywhere"
        },
        {
          "description": "The manifest's claiming extension declares no spelling that derives a\npackage from an import specifier (`DependencyIdentity::Underivable`), so\n\"nothing imports this dependency\" cannot be told from \"the imports spell\nit differently\" — JVM coordinates, Swift products, Python distributions.",
          "type": "string",
          "const": "specifier-identity-underivable"
        },
        {
          "description": "Files no extension claims sit inside the package with a suffix the\nclaiming extension declares can import\n(`ExtensionSpec::dependency_importers`) — a `.vue` component, an `.html`\npage — so an import of the dependency may exist where nothing can see it.",
          "type": "object",
          "properties": {
            "unclaimed-importers": {
              "type": "object",
              "properties": {
                "suffixes": {
                  "type": "array",
                  "items": {
                    "type": "string"
                  }
                }
              },
              "required": [
                "suffixes"
              ]
            }
          },
          "additionalProperties": false,
          "required": [
            "unclaimed-importers"
          ]
        },
        {
          "description": "No root reaches any file the package owns: dead or apparatus, its\ndependencies are moot and the file findings already say so.",
          "type": "string",
          "const": "nothing-reaches-owned-files"
        },
        {
          "description": "Every file the package owns is a test: a fixture package, whose\ndependencies serve the tests by construction.",
          "type": "string",
          "const": "owned-files-are-tests"
        },
        {
          "description": "No coverage report was ingested this run: a verdict with a measured\ncoverage factor cannot be reached for any function at once.",
          "type": "string",
          "const": "no-coverage-ingested"
        },
        {
          "description": "The ingested report never instrumented these files: their functions'\ncoverage is unknown, not zero.",
          "type": "string",
          "const": "no-coverage-record"
        }
      ]
    },
    "AbstentionScope": {
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "whole-run"
          ]
        },
        {
          "type": "object",
          "properties": {
            "files": {
              "type": "object",
              "properties": {
                "unmeasured": {
                  "type": "integer",
                  "format": "uint32",
                  "minimum": 0
                }
              },
              "required": [
                "unmeasured"
              ]
            }
          },
          "additionalProperties": false,
          "required": [
            "files"
          ]
        },
        {
          "description": "Dependency subjects only: this many declaring manifests went unjudged,\nevery file still judged as usual.",
          "type": "object",
          "properties": {
            "manifests": {
              "type": "object",
              "properties": {
                "unjudged": {
                  "type": "integer",
                  "format": "uint32",
                  "minimum": 0
                }
              },
              "required": [
                "unjudged"
              ]
            }
          },
          "additionalProperties": false,
          "required": [
            "manifests"
          ]
        }
      ]
    },
    "Categories": {
      "description": "Which categories this run JUDGES — never a display filter. An unselected\ncategory's analysis does not run: it leaves the `judged` set (so its\nsuppressions cannot read as stale), produces no abstention, and the report's\n`run.selection` says the narrowing was asked for. Health follows judgment —\nskip `unused` and health is absent, not padded. Plugin findings pass the same\ntest by category name; a plugin's [`Contribution`] still records what it\nasserted, because activity and selection are different facts.",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "all"
          ]
        },
        {
          "description": "Judge only these.",
          "type": "object",
          "properties": {
            "only": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/Category"
              }
            }
          },
          "additionalProperties": false,
          "required": [
            "only"
          ]
        },
        {
          "description": "Judge everything except these.",
          "type": "object",
          "properties": {
            "skip": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/Category"
              }
            }
          },
          "additionalProperties": false,
          "required": [
            "skip"
          ]
        }
      ]
    },
    "Category": {
      "description": "A finding category: a validated string newtype, not an enum, because the\n`ext:<coordinate>/<rule>` namespace is open. First-party categories are the\nassociated constants.",
      "type": "string"
    },
    "CategoryCount": {
      "type": "object",
      "properties": {
        "category": {
          "$ref": "#/$defs/Category"
        },
        "findings": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        }
      },
      "required": [
        "category",
        "findings"
      ]
    },
    "Confidence": {
      "description": "Ordered by strength, so `max()` and threshold comparisons read naturally.\nClosed by design: a fourth tier would change what every existing threshold means —\nthat is a semantic contract change, not growth.",
      "type": "string",
      "enum": [
        "possible",
        "probable",
        "certain"
      ]
    },
    "Contribution": {
      "description": "What one plugin asserted this run — the observable half of the containment\nmodel: a plugin can only lie about graph facts, and here is exactly what facts\nit asserted, what missed, and whether its content budget cut.",
      "type": "object",
      "properties": {
        "content_budget_cut": {
          "type": "boolean"
        },
        "coordinate": {
          "type": "string"
        },
        "dropped": {
          "description": "Contributions whose targets resolved to nothing, one described line each.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "findings": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "roots": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        }
      },
      "required": [
        "coordinate",
        "roots",
        "findings",
        "dropped",
        "content_budget_cut"
      ]
    },
    "CycleTolerance": {
      "description": "Whether an import cycle among this language's files is a defect worth a\nfinding — a LANGUAGE fact, declared as spec data so core never names a\nlanguage. `Hazard`: module-initialization order makes cycles bite (ESM/CJS\nTDZ and partially-initialized modules; Python's circular ImportError).\n`Tolerated` — the default — covers every silent reason at once: the\ncompiler forbids them (Go), resolves them routinely (JVM multi-pass), or\nthe module system makes them idiomatic (Rust modules within a crate);\ninformation is not dressed up as a defect, and silence needs no\nsub-classification to behave.",
      "type": "string",
      "enum": [
        "tolerated",
        "hazard"
      ]
    },
    "DependencyIdentity": {
      "description": "How an import specifier names a declared dependency — the ecosystem's\nspelling, as data: the engine matches every package-shaped import of an\nadapter's files against its manifests' declarations without calling back\ninto the adapter, and an adapter cannot claim to derive identity without\nsaying how. `unused` and `test-only` read it on their dependency subjects.",
      "oneOf": [
        {
          "description": "Specifiers name nothing this vocabulary can match with a declaration (a\nJVM artifact id names no package, a Swift package name is not its module\nname, a Python distribution is not its import name): every usage\njudgment abstains, and says so.",
          "type": "string",
          "const": "underivable"
        },
        {
          "description": "npm's spelling: the specifier is a package name — `@scope/name` or\n`name` — or a `/`-path under it (`lodash/fp` names `lodash`); a\n`@types/` declaration names the package it types (`@types/babel__core`\nnames `@babel/core`).",
          "type": "string",
          "const": "package-name"
        },
        {
          "description": "Go's spelling: the specifier is an import path and a declared module\npath prefixes it (`github.com/x/y/sub` names `github.com/x/y`). Where\nthe module boundary falls is the declaration's to say, so a path no\ndeclaration prefixes is reported whole.",
          "type": "string",
          "const": "module-path"
        },
        {
          "description": "Cargo's spelling: the specifier's leading `::` segment is the declared\nname with `-` spelled `_` (`serde_json::Value` names `serde-json`).",
          "type": "string",
          "const": "crate-root"
        }
      ]
    },
    "DependencyScoping": {
      "description": "How the ecosystem's manifests state a dependency's usage scope. `Scoped`\nmanifests have sections (npm `dependencies`/`devDependencies`, Cargo\n`[dev-dependencies]`), so a declaration with no scope is one the source could\nnot classify and no usage judgment reads it. `Unscoped` ecosystems have no\nsections at all — every direct requirement in `go.mod` is a build\nrequirement — so an unscoped declaration IS the usage claim, and \"only tests\nimport it\" has no section to move it to. `unused` and `test-only` are the\nconsumers, on their dependency subjects.",
      "type": "string",
      "enum": [
        "scoped",
        "unscoped"
      ]
    },
    "DiagnosticLevel": {
      "description": "Closed by design: three levels are the reporting contract.",
      "type": "string",
      "enum": [
        "info",
        "warn",
        "error"
      ]
    },
    "EvidenceStream": {
      "description": "An optional evidence stream — one whose absence would be ambiguous without a\ndeclaration. Grows a variant whenever a language teaches us a new stream\n(test spans, units, …); the default for every adapter is not-declared.",
      "type": "string",
      "enum": [
        "comments",
        "metrics"
      ]
    },
    "ExportNarrowing": {
      "description": "Whether the language offers an expressible visibility strictly below\n`Exported` — can a declaration stop being exported, by editing only itself,\nand keep compiling? TypeScript can drop `export`; a language whose only\nspelling IS the exported one cannot. The `internal-only` analysis is the\nconsumer: an exported declaration used only inside its own file is advice\nwhere narrowing is expressible and an impossibility where it is not.",
      "type": "string",
      "enum": [
        "none",
        "expressible"
      ]
    },
    "ExtensionRun": {
      "type": "object",
      "properties": {
        "dependency_identity": {
          "description": "How this extension's import specifiers name a declared dependency —\n`underivable` is why no dependency finding can exist for its manifests.",
          "$ref": "#/$defs/DependencyIdentity"
        },
        "dependency_scoping": {
          "description": "Whether the manifests this extension reads have dependency sections\n(`scoped`) or one flat requirement list (`unscoped`) — under `unscoped`,\n`test-only` never fires on a dependency: there is no section to move it to.",
          "$ref": "#/$defs/DependencyScoping"
        },
        "export_narrowing": {
          "description": "Whether the language can stop exporting a declaration by editing only\nit (`internal-only`'s Exported rung reads it).",
          "$ref": "#/$defs/ExportNarrowing"
        },
        "files": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "id": {
          "type": "string"
        },
        "import_cycles": {
          "description": "Whether the language calls import cycles a hazard (`cyclic` reads it);\n`tolerated` is why a cycle-free-by-compiler language reports none.",
          "$ref": "#/$defs/CycleTolerance"
        },
        "narrowable_scopes": {
          "description": "The judgment capabilities this extension declared — the one-row answer\nto \"why does kndo (not) report X for this language\". Scope tokens its\nladder can demote (`internal-only`'s Scoped rung reads it); empty means\nthat rung never fires here.",
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      },
      "required": [
        "id",
        "files",
        "export_narrowing",
        "import_cycles",
        "dependency_scoping",
        "dependency_identity"
      ]
    },
    "Finding": {
      "type": "object",
      "properties": {
        "category": {
          "$ref": "#/$defs/Category"
        },
        "confidence": {
          "$ref": "#/$defs/Confidence"
        },
        "id": {
          "$ref": "#/$defs/FindingId"
        },
        "lines": {
          "description": "Where the subject's span sits in its file, when the subject has one.\nNever identity: moving code changes lines and must not change the finding.",
          "anyOf": [
            {
              "$ref": "#/$defs/LineSpan"
            },
            {
              "type": "null"
            }
          ]
        },
        "message": {
          "type": "string"
        },
        "severity": {
          "$ref": "#/$defs/Severity"
        },
        "subject": {
          "$ref": "#/$defs/Subject"
        }
      },
      "required": [
        "id",
        "category",
        "severity",
        "confidence",
        "subject",
        "message"
      ]
    },
    "FindingId": {
      "description": "A finding's stable identity. Baselines and suppressions depend on it, so it is a\ntype, derived from typed parts — never five adjacent strings.",
      "type": "string"
    },
    "Health": {
      "type": "object",
      "properties": {
        "by_category": {
          "description": "Counting findings per category, in category order. More entries than\n`implicated` when one subject carries findings from several categories.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/CategoryCount"
          }
        },
        "by_package": {
          "description": "The same two integers partitioned by owning package (name-sorted; the\nempty name is the unpackaged remainder). Filled by\n[`Health::partition`] where a graph is in hand; absent on\ngraph-free measurements.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/PackageHealth"
          }
        },
        "implicated": {
          "description": "Distinct universe subjects carrying at least one counting finding.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "subjects": {
          "description": "The judged universe: every declaration plus every claimed file.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        }
      },
      "required": [
        "implicated",
        "subjects",
        "by_category"
      ]
    },
    "LineSpan": {
      "description": "1-based, inclusive line range — display data the engine derives from the\nsubject's byte span against the file's actual newlines. Lines are exact in any\nencoding (a newline is one byte); columns are deliberately absent — every\ninterchange format counts them in its own unit, and a slightly-wrong column is\nworse than none.",
      "type": "object",
      "properties": {
        "end": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "start": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        }
      },
      "required": [
        "start",
        "end"
      ]
    },
    "Mode": {
      "description": "What this report's `findings`/`fixed` split was computed against. `full` is a\ntree against its baseline file (all current findings when none exists); the\ndiff modes compare two TREES — base never includes the baseline file, so a\nbaselined finding reintroduced by a change reads as new debt.",
      "oneOf": [
        {
          "description": "The whole tree, split against the on-disk baseline when one exists.",
          "type": "string",
          "const": "full"
        },
        {
          "description": "What `git commit` would commit (the index) against HEAD.",
          "type": "string",
          "const": "staged"
        },
        {
          "description": "The worktree against the merge-base with a named ref.",
          "type": "string",
          "const": "diff"
        }
      ]
    },
    "PackageHealth": {
      "type": "object",
      "properties": {
        "implicated": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "manifest": {
          "description": "The anchoring manifest — what tells two same-named packages apart\n(parallel trees legitimately duplicate names); empty for the\nunpackaged bucket.",
          "type": "string"
        },
        "name": {
          "description": "The package's declared name; empty for subjects outside every package.",
          "type": "string"
        },
        "subjects": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        }
      },
      "required": [
        "name",
        "manifest",
        "implicated",
        "subjects"
      ]
    },
    "ProjectPath": {
      "description": "A `/`-separated, project-relative path. The one path spelling that crosses crate\nboundaries; OS paths stay at the discovery edge.",
      "type": "string"
    },
    "ReportDiagnostic": {
      "type": "object",
      "properties": {
        "level": {
          "$ref": "#/$defs/DiagnosticLevel"
        },
        "message": {
          "type": "string"
        },
        "path": {
          "$ref": "#/$defs/ProjectPath"
        }
      },
      "required": [
        "path",
        "level",
        "message"
      ]
    },
    "RunInfo": {
      "type": "object",
      "properties": {
        "extensions": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/ExtensionRun"
          }
        },
        "files_claimed": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "files_discovered": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "mode": {
          "$ref": "#/$defs/Mode"
        },
        "schema": {
          "description": "Stamped as a CONST in the generated schema, from the same `REPORT_SCHEMA` the report\nwrites — a report from any other envelope version fails validation instead of\ndrifting through.",
          "const": "kndo-v2/m6"
        },
        "selection": {
          "description": "The category narrowing this run was asked for, verbatim — absent when the\nrun judged everything. What a narrowed run does not list, it did not judge.",
          "anyOf": [
            {
              "$ref": "#/$defs/Categories"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "required": [
        "schema",
        "mode",
        "files_discovered",
        "files_claimed",
        "extensions"
      ]
    },
    "Severity": {
      "description": "Declared worst-first so `Ord` is display order; gates compare via\n[`Severity::at_least`], the one place the inversion lives.",
      "type": "string",
      "enum": [
        "error",
        "warning",
        "info"
      ]
    },
    "Span": {
      "description": "A byte range within one file: `start..end`, end-exclusive. Bytes, not line/column —\ntree-sitter yields them for free, overlap and containment are arithmetic, and a\ncentral line index renders line/column only at the output edge.",
      "type": "object",
      "properties": {
        "end": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "start": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        }
      },
      "required": [
        "start",
        "end"
      ]
    },
    "Subject": {
      "oneOf": [
        {
          "type": "object",
          "properties": {
            "kind": {
              "type": "string",
              "const": "file"
            },
            "path": {
              "$ref": "#/$defs/ProjectPath"
            }
          },
          "required": [
            "kind",
            "path"
          ]
        },
        {
          "type": "object",
          "properties": {
            "kind": {
              "type": "string",
              "const": "symbol"
            },
            "path": {
              "$ref": "#/$defs/ProjectPath"
            },
            "selector": {
              "$ref": "#/$defs/SymbolSelector"
            },
            "span": {
              "$ref": "#/$defs/Span"
            }
          },
          "required": [
            "kind",
            "path",
            "selector",
            "span"
          ]
        },
        {
          "type": "object",
          "properties": {
            "kind": {
              "type": "string",
              "const": "package"
            },
            "manifest": {
              "$ref": "#/$defs/ProjectPath"
            },
            "name": {
              "type": "string"
            }
          },
          "required": [
            "kind",
            "manifest",
            "name"
          ]
        },
        {
          "type": "object",
          "properties": {
            "kind": {
              "type": "string",
              "const": "dependency"
            },
            "name": {
              "type": "string"
            },
            "owner_manifest": {
              "$ref": "#/$defs/ProjectPath"
            }
          },
          "required": [
            "kind",
            "owner_manifest",
            "name"
          ]
        },
        {
          "type": "object",
          "properties": {
            "kind": {
              "type": "string",
              "const": "directory"
            },
            "path": {
              "$ref": "#/$defs/ProjectPath"
            }
          },
          "required": [
            "kind",
            "path"
          ]
        },
        {
          "description": "One import statement, addressed by the specifier AS WRITTEN — identity\nsurvives the code moving (the span is carried for lines, never for\nidentity, the same rule Symbol follows).",
          "type": "object",
          "properties": {
            "kind": {
              "type": "string",
              "const": "import"
            },
            "path": {
              "$ref": "#/$defs/ProjectPath"
            },
            "span": {
              "$ref": "#/$defs/Span"
            },
            "specifier": {
              "type": "string"
            }
          },
          "required": [
            "kind",
            "path",
            "specifier",
            "span"
          ]
        },
        {
          "type": "object",
          "properties": {
            "kind": {
              "type": "string",
              "const": "suppression"
            },
            "path": {
              "$ref": "#/$defs/ProjectPath"
            },
            "span": {
              "$ref": "#/$defs/Span"
            }
          },
          "required": [
            "kind",
            "path",
            "span"
          ]
        }
      ]
    },
    "SuppressedSummary": {
      "description": "What suppression did to a run, for the report: totals only — the suppressed\nfindings themselves are the pragmas' business, not the envelope's.",
      "type": "object",
      "properties": {
        "by_category": {
          "type": "array",
          "items": {
            "type": "array",
            "maxItems": 2,
            "minItems": 2,
            "prefixItems": [
              {
                "$ref": "#/$defs/Category"
              },
              {
                "type": "integer",
                "format": "uint32",
                "minimum": 0
              }
            ]
          }
        },
        "total": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        }
      },
      "required": [
        "total",
        "by_category"
      ]
    },
    "SymbolSelector": {
      "description": "Names a symbol within a file. The one place the member-of relation is spelled;\nrendering (`Owner.name`) is the output edge's job, never a parsing convention.",
      "oneOf": [
        {
          "type": "object",
          "properties": {
            "Free": {
              "type": "string"
            }
          },
          "additionalProperties": false,
          "required": [
            "Free"
          ]
        },
        {
          "type": "object",
          "properties": {
            "Member": {
              "type": "object",
              "properties": {
                "name": {
                  "type": "string"
                },
                "owner": {
                  "type": "string"
                }
              },
              "required": [
                "owner",
                "name"
              ]
            }
          },
          "additionalProperties": false,
          "required": [
            "Member"
          ]
        }
      ]
    }
  }
}
